namespace CrmWebResourcesUpdater.Common
{
    /// <summary>
    /// Command IDs for commands edded by VSIX extension
    /// </summary>
    public enum Commands
    {
        UpdateWebresources = 0x100,
        OpenConfiguration = 0x103,
    }
}
